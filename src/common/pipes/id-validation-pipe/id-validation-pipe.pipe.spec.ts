import { IdValidationPipePipe } from './id-validation-pipe.pipe.js';

describe('IdValidationPipePipe', () => {
  it('should be defined', () => {
    expect(new IdValidationPipePipe()).toBeDefined();
  });
});
